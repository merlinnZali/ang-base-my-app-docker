export const APP_NAME = 'TestApp'
export const VERSION = '0.0.1'

export interface Environment {
    appName: string
    version: string
    env: string
    assetsPath: string
    configPath: string
}
